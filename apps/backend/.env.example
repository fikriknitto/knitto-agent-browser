# =============================================================================
# Server
# =============================================================================
BACKEND_HOST=0.0.0.0
BACKEND_PORT=3080

# =============================================================================
# Agent credentials — Web UI only (Settings → Agent credentials)
# Do NOT set API keys here; they are pushed over WebSocket from localStorage.
# =============================================================================

# =============================================================================
# Bridge (non-credential)
# =============================================================================
KNITTO_BRIDGE_MODEL=composer-2.5
KNITTO_BRIDGE_MAX_CONCURRENT=1
KNITTO_BRIDGE_JOB_TIMEOUT_MS=600000
KNITTO_BRIDGE_MAX_TOOL_CALLS=25

# =============================================================================
# Browser / Playwright
# =============================================================================
AUTOMATION_HEADLESS=false
AUTOMATION_VIEWPORT_WIDTH=1366
AUTOMATION_VIEWPORT_HEIGHT=768
# AUTOMATION_SNAPSHOT_MAX_ELEMENTS=100
AUTOMATION_RECORD_VIDEO=true
AUTOMATION_RECORD_FPS=25
AUTOMATION_VIDEO_SPEED=1.5
# AUTOMATION_VIDEO_TRIM_IDLE=true
# PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=
# ffmpeg dibutuhkan untuk AUTOMATION_VIDEO_SPEED / AUTOMATION_VIDEO_TRIM_IDLE.
# Kosongkan kalau ffmpeg sudah ada di PATH. Jangan pakai ffmpeg bawaan Playwright
# (ms-playwright/ffmpeg-*): build itu tanpa mpdecimate/setpts dan tanpa encoder H.264.
# AUTOMATION_FFMPEG_PATH=C:/ffmpeg/bin/ffmpeg.exe
# Docker production (otomatis di docker.env):
# AUTOMATION_MCP_COMMAND=node
# AUTOMATION_MCP_PATH=apps/backend/dist/platforms/browser/mcp-stdio-server.js

# =============================================================================
# File manager + local Worker workspace (storage/)
# =============================================================================
STORAGE_ROOT=./storage
STORAGE_MAX_UPLOAD_BYTES=52428800
# Job evidence workspace defaults to {STORAGE_ROOT}/agents/{jobId}/
# (mid-job + upload buffer; durable results = API Data / MinIO).
# AUTOMATION_SCREENSHOT_DIR overrides the PARENT of that "agents/{jobId}/" tree
# (i.e. set it to something like ./storage, not ./storage/agents — the "agents"
# segment is always appended on top of this value).

# =============================================================================
# OpenAI-compatible agent (retry tuning only — Base URL / API key via Web UI)
# =============================================================================
OPENAI_COMPAT_MAX_RETRIES=5
OPENAI_COMPAT_RETRY_DELAY_MS=2000

# Evidence upload harden (W7)
# API_DATA_UPLOAD_MAX_RETRIES=5
# API_DATA_UPLOAD_RETRY_DELAY_MS=500
# EVIDENCE_UPLOAD_FLUSH_MS=60000

# GitHub Packages (@knittotextile/knitto-agent-*): set NODE_AUTH_TOKEN for pnpm install
# (e.g. export NODE_AUTH_TOKEN=$(gh auth token) with read:packages scope)

# =============================================================================
# Mobile / Appium (Android)
# Prasyarat: Appium server + ADB (adb devices). Rekaman video mobile memakai
# Appium startRecordingScreen/stopRecordingScreen — bukan ffmpeg.
# =============================================================================
APPIUM_SERVER_URL=http://127.0.0.1:4723
# MOBILE_UDID=emulator-5554
# MOBILE_DEVICE_UDIDS=emulator-5554,emulator-5556
MOBILE_DEVICE_POOL_ENABLED=true
MOBILE_DEVICE_ACQUIRE_TIMEOUT_MS=60000
MOBILE_IMPLICIT_WAIT_MS=5000
MOBILE_SNAPSHOT_MAX_ELEMENTS=100
MOBILE_RECORD_VIDEO=true
# Satu test case = satu video (tc-01.mp4, tc-02.mp4, …) untuk job hybrid/multi-TC
RECORD_VIDEO_PER_TEST_CASE=true
MOBILE_RECORD_TIME_LIMIT_SEC=600
MOBILE_RECORD_FPS=20
MOBILE_RECORD_BIT_RATE=4000000
# MOBILE_VIDEO_FILENAME=recording.mp4
# Interval polling adb devices untuk SSE /api/mobile/devices/stream (ms)
MOBILE_DEVICES_POLL_MS=3000
# TTL cache daftar package per device (ms) — kurangi beban adb pm list
MOBILE_PACKAGES_CACHE_TTL_MS=60000
# MOBILE_MEMORY_DIR=memory/mobile
# MOBILE_UPLOAD_DIR=storage/mobile-uploads
# MOBILE_UPLOAD_MAX_BYTES=52428800
# MOBILE_MCP_PATH=apps/backend/dist/mobile-automation/mcp-stdio-server.js

# =============================================================================
# BlueStacks (Windows) — pnpm start:instances / connect:instances
# Dipakai script di scripts/bluestacks/ (bukan runtime backend).
# =============================================================================
# BLUESTACKS_INSTALL_DIR=C:/Program Files/BlueStacks_nxt
# BLUESTACKS_DATA_DIR=C:/ProgramData/BlueStacks_nxt
# BLUESTACKS_CONF_PATH=
# BLUESTACKS_PLAYER_PATH=
BLUESTACKS_ADB_HOST=127.0.0.1
BLUESTACKS_ADB_CONNECT_DELAY_MS=8000

# =============================================================================
# Logging
# =============================================================================
KNITTO_MCP_LOG_LEVEL=info

# =============================================================================
# API Data (Knitto Api Automation QA Data) — W2 job lifecycle
# Worker forwards user JWT from WS user_prompt.apiDataToken (AUTH MVP).
# =============================================================================
API_DATA_BASE_URL=http://localhost:8009
# Stale RUNNING TTL (ms). Resume mid-job tidak didukung; mark ERROR stale_running.
# Default 6 jam = 21600000
# AGENT_RUN_STALE_MS=21600000
